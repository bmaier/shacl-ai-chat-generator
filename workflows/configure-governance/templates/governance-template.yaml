# BSG Governance Configuration
# Generated by Master Lex
date: {{date}}
user: {{user_name}}

base_url: {{base_url}}
local_data_path: {{local_resource_path}}

ontologies:
{{#each ontologies}}
  - name: {{this.name}}
    path: {{this.path}}
    prefix: {{this.prefix}}
{{/each}}

naming_convention:
  pattern: "{{naming_pattern}}"
  rationale: "{{naming_rationale}}"
